CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
#PROJECT()
#ADD_SUBDIRECTORY(src)
ADD_DEFINITIONS(-std=c++11)
FIND_PACKAGE(OpenCV REQUIRED)
#INCLUDE_DIRECTORIES()
#LINK_DIRECTORIES()
ADD_EXECUTABLE(generate_gridboard generate_gridboard.cpp )
TARGET_LINK_LIBRARIES(generate_gridboard  ${OpenCV_LIBS} )

